# Production domain. Injected into canonical URLs, og:url, sitemap and robots
# at build time so no production URL is ever committed.
SITE_URL=https://bluegolddaily.com

# The browser-visible Formspree delivery endpoint is configured in join.html.
# It is public configuration, not a secret or an environment variable.
